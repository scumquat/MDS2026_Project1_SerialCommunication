import serial
port = "COM12"
baud = 9600
ser = serial.Serial(port, baud, timeout=1)

try:
    while True:
        if ser.in_waiting > 0:
            line = ser.readline().decode('utf-8').rstrip()
            print(line)
except KeyboardInterrupt:
    print("\nExiting...")
finally:
    ser.close()